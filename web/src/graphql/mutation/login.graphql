mutation login(
    $usernameOrEmail: String!
    $password: String!
    ) {
    login (
        usernameOrEmail: $usernameOrEmail,
        password: $password
    ) {
        username
        email
        password
    }
}