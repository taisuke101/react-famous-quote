query getQuote(
    $author: String,
    $country: String,
    $job: String
) {
    getQuote(
        author: $author,
        country: $country,
        job: $job
    ) {
        ...quoteResponse
    }
}