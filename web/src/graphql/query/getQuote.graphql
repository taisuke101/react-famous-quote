query getQuote(
    $author: String,
    $country: String,
    $job: String,
    $category: String,
) {
    getQuote(
        author: $author,
        country: $country,
        job: $job,
        category: $category
    ) {
        ...quoteResponse
    }
}